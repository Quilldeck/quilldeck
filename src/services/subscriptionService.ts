export const TAOSCOPE_WALLET = 'demo_wallet';
export const SUBSCRIPTION_PRICE_USDC = 4.99;

export const subscriptionService = {
  activatePro: async (wallet: string, tx: string) => {
    return { success: true };
  }
};
